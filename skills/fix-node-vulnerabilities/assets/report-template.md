# Dependency Vulnerability Upgrade Report

> Generated by `fix-node-vulnerabilities`. Review and approve before any change is applied.

- **Repo:** `<repo>`
- **Date:** `<YYYY-MM-DD>`
- **Source of findings:** `<npm audit json | dependabot | snyk | screenshot>`
- **Projects affected:** `<dynamic-app/, functions/, …>`

## Summary

| # | Package | Severity | Type | Upgrade target | Current → New | Upgrade type | Refactor needed | Project |
|---|---------|----------|------|----------------|---------------|--------------|-----------------|---------|
| 1 | `<pkg>` | `<crit/high/mod/low>` | direct | `<pkg>` | `1.2.3 → 1.2.7` | patch | no | `<project>` |
| 2 | `<pkg>` | `<…>` | transitive | `<parent-pkg>` (or `overrides` pin) | `4.0.0 → 5.1.0` | major | yes | `<project>` |

Legend — **Type:** direct = listed in package.json; transitive = pulled in by a parent (target is
the parent to upgrade, or an `overrides` pin). **Upgrade type:** patch / minor / major per semver.

## Findings detail

### 1. `<package>` — `<advisory id / CVE>` (`<severity>`)

- **Type:** direct | transitive
- **Path (if transitive):** `<parent> > … > <vulnerable-pkg>`
- **Upgrade target:** `<pkg>` `<current>` → `<new>`  (or: `overrides` pin `<pkg>@<patched>`)
- **Upgrade type:** patch | minor | major
- **Why this version:** lowest release satisfying patched range `<range>`.

#### Breaking changes (major upgrades only)

| Breaking change | Codebase impacted? | Affected files / notes |
|-----------------|--------------------|------------------------|
| `<removed/renamed API, changed default, dropped Node, ESM-only, …>` | [ ] impacted / [x] not impacted | `<src/...>` |
| `<…>` | [ ] / [x] | `<…>` |

**Refactor plan (if any box is impacted):** `<concise description of the behavior-preserving change>`

---

### 2. `<package>` — …

(repeat per finding)

## No-fix / risk-accept findings

| Package | Advisory | Reason no clean fix | Recommended mitigation |
|---------|----------|---------------------|------------------------|
| `<pkg>` | `<id>` | `<no patch released / only major breaks build>` | `<override to nearest safe / remove dep / accept>` |

## Implementation plan

1. Apply version changes / `overrides` listed above.
2. Refresh lockfile(s) with `npm install` per project.
3. Refactor impacted breaking changes (behavior-preserving).
4. Run lint / test / build / `npm audit` per project; confirm findings cleared.
5. Apply repo conventions (semver bump, branch + commit prefix).
6. Open PR linking this report.

**Implementation mode:** `<standard | dynamic workflow>` (chosen by operator)
